<h1 class="Topich1">Users</h1></div>

{{#each users}}
	<div class="single box">
		<h3>
			<a href="/users/{{id}}">{{name}}</a>
		</h3>

		<a href="/users/{{id}}">
			<img src="{{img_url}}" alt="" height="200" width="200">
		</a>

	</div>
{{/each}}

